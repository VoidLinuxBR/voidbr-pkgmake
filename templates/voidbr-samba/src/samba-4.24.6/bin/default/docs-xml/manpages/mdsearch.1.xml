<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE refentry PUBLIC "-//OASIS//DTD DocBook XML V4.2//EN" "http://www.oasis-open.org/docbook/xml/4.2/docbookx.dtd">
<refentry id="mdsearch.1">

  <refmeta>
    <refentrytitle>mdsearch</refentrytitle>
    <manvolnum>1</manvolnum>
    <refmiscinfo class="source">Samba</refmiscinfo>
    <refmiscinfo class="manual">User Commands</refmiscinfo>
    <refmiscinfo class="version">4.24.6</refmiscinfo>
  </refmeta>

  <refnamediv>
    <refname>mdsearch</refname>
    <refpurpose>Run Spotlight searches against an SMB server</refpurpose>
  </refnamediv>

  <refsynopsisdiv>
    <cmdsynopsis sepchar=" ">
      <literal>mdfine</literal>
      <arg choice="req" rep="norepeat">server</arg>
      <arg choice="req" rep="norepeat">sharename</arg>
      <arg choice="req" rep="norepeat">query</arg>
      <arg choice="opt" rep="norepeat">-p, --path=STRING</arg>
      <arg choice="opt" rep="norepeat">-L, --live</arg>
      <arg choice="opt" rep="norepeat">-?|--help</arg>
      <arg choice="opt" rep="norepeat">--usage</arg>
      <arg choice="opt" rep="norepeat">-d|--debuglevel=DEBUGLEVEL</arg>
      <arg choice="opt" rep="norepeat">--debug-stdout</arg>
      <arg choice="opt" rep="norepeat">--configfile=CONFIGFILE</arg>
      <arg choice="opt" rep="norepeat">--option=name=value</arg>
      <arg choice="opt" rep="norepeat">-l|--log-basename=LOGFILEBASE</arg>
      <arg choice="opt" rep="norepeat">--leak-report</arg>
      <arg choice="opt" rep="norepeat">--leak-report-full</arg>
      <arg choice="opt" rep="norepeat">-R|--name-resolve=NAME-RESOLVE-ORDER</arg>
      <arg choice="opt" rep="norepeat">-O|--socket-options=SOCKETOPTIONS</arg>
      <arg choice="opt" rep="norepeat">-m|--max-protocol=MAXPROTOCOL</arg>
      <arg choice="opt" rep="norepeat">-n|--netbiosname=NETBIOSNAME</arg>
      <arg choice="opt" rep="norepeat">--netbios-scope=SCOPE</arg>
      <arg choice="opt" rep="norepeat">-W|--workgroup=WORKGROUP</arg>
      <arg choice="opt" rep="norepeat">--realm=REALM</arg>
      <arg choice="opt" rep="norepeat">-U|--user=[DOMAIN/]USERNAME[%PASSWORD]</arg>
      <arg choice="opt" rep="norepeat">-N|--no-pass</arg>
      <arg choice="opt" rep="norepeat">--password=STRING</arg>
      <arg choice="opt" rep="norepeat">--pw-nt-hash</arg>
      <arg choice="opt" rep="norepeat">-A|--authentication-file=FILE</arg>
      <arg choice="opt" rep="norepeat">-P|--machine-pass</arg>
      <arg choice="opt" rep="norepeat">--simple-bind-dn=DN</arg>
      <arg choice="opt" rep="norepeat">--use-kerberos=desired|required|off</arg>
      <arg choice="opt" rep="norepeat">--use-krb5-ccache=CCACHE</arg>
      <arg choice="opt" rep="norepeat">--use-winbind-ccache</arg>
      <arg choice="opt" rep="norepeat">--client-protection=sign|encrypt|off</arg>
      <arg choice="opt" rep="norepeat">-V|--version</arg>
    </cmdsynopsis>
  </refsynopsisdiv>

  <refsect1>
    <title>DESCRIPTION</title>

    <para>This tool is part of the <citerefentry><refentrytitle>samba</refentrytitle>
    <manvolnum>1</manvolnum></citerefentry> suite.</para>

    <para>mdsearch is a simple utility to run Spotlight searches against an SMB server
    that runs the Spotlight <emphasis>mdssvc</emphasis> RPC service.</para>
  </refsect1>

  <refsect1>
    <title>OPTIONS</title>

    <variablelist>
      <varlistentry>
	<term>server</term>
	<listitem>
	  <para>The SMB server name or IP address to connect to.</para>
	</listitem>
      </varlistentry>

      <varlistentry>
	<term>sharename</term>
	<listitem>
	  <para>The name of a share on the server.</para>
	</listitem>
      </varlistentry>

      <varlistentry>
	<term>query</term>
	<listitem>
	  <para>The query expression syntax is a simplified form of filename
	  globbing familiar to shell users. Queries have the following
	  format:</para>

	  <para>attribute=="value"</para>

	  <para>For queries against a Samba server with Spotlight enabled using
	  the Elasticsearch backend, the list of supported metadata attributes
	  is given by the JSON attribute mapping file, typically installed at
	  <filename moreinfo="none">/usr/share/samba/mdssvc/elasticsearch_mappings.json</filename>
	  </para>
	</listitem>
      </varlistentry>

      <varlistentry>
	<term>-p PATH, --path=PATH</term>
	<listitem>
	  <para>Server side path to search, defaults to
	  <emphasis>"/"</emphasis></para>
	</listitem>
      </varlistentry>

      <varlistentry>
	<term>-L, --live</term>
	<listitem><para>Query remains running.</para></listitem>
      </varlistentry>

      

<varlistentry>
<term>-?|--help</term>
<listitem><para>Print a summary of command line options.
</para></listitem>
</varlistentry>


<varlistentry>
<term>--usage</term>
<listitem><para>Display brief usage message.
</para></listitem>
</varlistentry>


      

<varlistentry>
	<term>-d|--debuglevel=level</term>
	<listitem>
		<para>
			<replaceable>level</replaceable> is an integer from 0
			to 10. The default value if this parameter is not
			specified is 1 for client applications.
		</para>

		<para>
			The higher this value, the more detail will be logged
			to the log files about the activities of the server. At
			level 0, only critical errors and serious warnings will
			be logged. Level 1 is a reasonable level for day-to-day
			running - it generates a small amount of information
			about operations carried out.
		</para>

		<para>
			Levels above 1 will generate considerable amounts of
			log data, and should only be used when investigating a
			problem. Levels above 3 are designed for use only by
			developers and generate HUGE amounts of log data, most
			of which is extremely cryptic.
			</para>

		<para>
			Note that specifying this parameter here will override
			the <link xmlns:xlink="http://www.w3.org/1999/xlink" linkend="LOGLEVEL" xlink:href="smb.conf.5.html#LOGLEVEL">log level</link> parameter in the
			<filename moreinfo="none">/opt/samba/etc/smb.conf</filename> file.
		</para>
	</listitem>
</varlistentry>

<varlistentry>
	<term>--debug-stdout</term>
	<listitem>
		<para>
			This will redirect debug output to STDOUT. By default
			all clients are logging to STDERR.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>--configfile=&lt;configuration file&gt;</term>
	<listitem>
		<para>
			The file specified contains the configuration details
			required by the client. The information in this file
			can be general for client and server or only provide
			client specific like options such as
			<link xmlns:xlink="http://www.w3.org/1999/xlink" linkend="CLIENTSMBENCRYPT" xlink:href="smb.conf.5.html#CLIENTSMBENCRYPT">client smb encrypt</link>. See
			<filename moreinfo="none">/opt/samba/etc/smb.conf</filename> for more information.  The default
			configuration file name is determined at compile time.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>--option=&lt;name&gt;=&lt;value&gt;</term>
	<listitem>
		<para>
			Set the
			<citerefentry><refentrytitle>smb.conf</refentrytitle>
			<manvolnum>5</manvolnum></citerefentry> option
			"&lt;name&gt;" to value "&lt;value&gt;" from the
			command line. This overrides compiled-in defaults and
			options read from the configuration file. If a name or
			a value includes a space, wrap whole
			--option=name=value into quotes.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>-l|--log-basename=logdirectory</term>
	<listitem>
		<para>
			Base directory name for log/debug files. The extension
			<constant>".progname"</constant> will be appended (e.g.
			log.smbclient, log.smbd, etc...). The log file is never
			removed by the client.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>--leak-report</term>
	<listitem>
		<para>
			Enable talloc leak reporting on exit.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>--leak-report-full</term>
	<listitem>
		<para>
			Enable full talloc leak reporting on exit.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>-V|--version</term>
	<listitem>
		<para>
			Prints the program version number.
		</para>
	</listitem>
</varlistentry>


      

<varlistentry>
	<term>-U|--user=[DOMAIN\]USERNAME[%PASSWORD]</term>
	<listitem>
		<para>
			Sets the SMB username or username and password.
		</para>

		<para>
			If %PASSWORD is not specified, the user will be
			prompted. The client will first check the
			<envar>USER</envar> environment variable
			(which is also permitted to also contain the
			password separated by a %), then the
			<envar>LOGNAME</envar> variable (which is not
			permitted to contain a password) and if either exists,
			the value is used. If these environmental
			variables are not found, the username
			found in a Kerberos Credentials cache may be used.
		</para>

		<para>
			A third option is to use a credentials file which
			contains the plaintext of the username and password.
			This option is mainly provided for scripts where the
			admin does not wish to pass the credentials on the
			command line or via environment variables. If this
			method is used, make certain that the permissions on
			the file restrict access from unwanted users.  See the
			<parameter moreinfo="none">-A</parameter> for more details.
		</para>

		<para>
			Be cautious about including passwords in scripts
			or passing user-supplied values onto the command line. For
			security it is better to let the Samba client tool ask for the
			password if needed, or obtain the password once with <literal>kinit</literal>.
		</para>
		<para>
			While Samba will attempt to scrub the password
			from the process title (as seen in ps), this
			is after startup and so is subject to a race.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>-N|--no-pass</term>
	<listitem>
		<para>
			If specified, this parameter suppresses the normal
			password prompt from the client to the user. This is
			useful when accessing a service that does not require a
			password.
		</para>

		<para>
			Unless a password is specified on the command line or
			this parameter is specified, the client will request a
			password.
		</para>

		<para>
			If a password is specified on the command line and this
			option is also defined the password on the command line
			will be silently ignored and no password will be
			used.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>--password</term>
	<listitem>
		<para>
			Specify the password on the commandline.
		</para>

		<para> Be cautious about including passwords in
			scripts or passing user-supplied values onto
			the command line. For security it is better to
			let the Samba client tool ask for the password
			if needed, or obtain the password once with
			<literal>kinit</literal>.
		</para>

		<para> If --password is not specified,
		       the tool will check the <envar>PASSWD</envar>
		       environment variable, followed by <envar>PASSWD_FD</envar>
		       which is expected to contain an open
		       file descriptor (FD) number.
		</para>
		<para>
		       Finally it will check <envar>PASSWD_FILE</envar> (containing
		       a file path to be opened). The file should only
		       contain the password. Make certain that the
		       permissions on the file restrict
		       access from unwanted users!
		</para>
		<para>
			While Samba will attempt to scrub the password
			from the process title (as seen in ps), this
			is after startup and so is subject to a race.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>--pw-nt-hash</term>
	<listitem>
		<para>
			The supplied password is the NT hash.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>-A|--authentication-file=filename</term>
	<listitem>
		<para>
			This option allows you to specify a file from which to
			read the username and password used in the connection.
			The format of the file is:
		</para>

		<para>
			<programlisting format="linespecific">
				username = &lt;value&gt;
				password = &lt;value&gt;
				domain   = &lt;value&gt;
			</programlisting>
		</para>

		<para>
			Make certain that the permissions on the file restrict
			access from unwanted users!
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>-P|--machine-pass</term>
	<listitem>
		<para>
			Use stored machine account password.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>--simple-bind-dn=DN</term>
	<listitem>
		<para>
			DN to use for a simple bind.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>--use-kerberos=desired|required|off</term>
	<listitem>
		<para>
			This parameter determines whether Samba client tools
			will try to authenticate using Kerberos. For Kerberos
			authentication you should use DNS names instead of IP
			addresses when connecting to a service.

			By default Samba client tools will try to use the
			default Kerberos credential cache (ccache).
			In case the ccache does not exist or -U|--user option
			was specified, clients will ask to enter a password and
			will obtain a Kerberos ticket (kinit) for you.

			If you want to use an alternative Kerberos credentical
			cache, use the --use-krb5-ccache option.
		</para>

		<para>
			Note that specifying this parameter here will override
			the <link xmlns:xlink="http://www.w3.org/1999/xlink" linkend="CLIENTUSEKERBEROS" xlink:href="smb.conf.5.html#CLIENTUSEKERBEROS">client use kerberos</link>
			parameter in the <filename moreinfo="none">/opt/samba/etc/smb.conf</filename> file.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>--use-krb5-ccache=CCACHE</term>
	<listitem>
		<para>
			Specifies the credential cache location for Kerberos
			authentication.
		</para>

		<para>
			This will enforce --use-kerberos=required.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>--use-winbind-ccache</term>
	<listitem>
		<para>
			Try to use the credential cache by winbind.
		</para>
	</listitem>
</varlistentry>


<varlistentry>
	<term>--client-protection=sign|encrypt|off</term>
	<listitem>
		<para>
			Sets the connection protection the client tool should
			use.
		</para>

		<para>
			Note that specifying this parameter here will override
			the <link xmlns:xlink="http://www.w3.org/1999/xlink" linkend="CLIENTPROTECTION" xlink:href="smb.conf.5.html#CLIENTPROTECTION">client protection</link>
			parameter in the <filename moreinfo="none">/opt/samba/etc/smb.conf</filename> file.
		</para>

		<para>
			In case you need more fine grained control you can use:
			<literal>--option=clientsmbencrypt=OPTION</literal>,
			<literal>--option=clientipcsigning=OPTION</literal>,
			<literal>--option=clientsigning=OPTION</literal>.
		</para>
	</listitem>
</varlistentry>


    </variablelist>
  </refsect1>

  <refsect1>
    <title>EXAMPLES</title>

    <para>Search all indexed metadata attributes, exact match:</para>
    <programlisting format="linespecific">
      '*=="Samba"'
</programlisting>

    <para>Search all indexed metadata attributes, prefix match:</para>
    <programlisting format="linespecific">
      '*=="Samba*"'
</programlisting>

    <para>Search by filename:</para>
    <programlisting format="linespecific">
      'kMDItemFSName=="Samba*"'
</programlisting>

    <para>Search by date:</para>
    <programlisting format="linespecific">
      'kMDItemFSContentChangeDate&lt;$time.iso(2018-10-01T10:00:00Z)'
</programlisting>

    <para>Search files's content:</para>
    <programlisting format="linespecific">
      'kMDItemTextContent=="Samba*"'
</programlisting>

    <para>Expressions:</para>
    <programlisting format="linespecific">
      kMDItemFSName=="Samba*"||kMDItemTextContent=="Tango*"'
</programlisting>
  </refsect1>

  <refsect1>
    <title>SEE ALSO</title>

    <para>File Metadata Search Programming Guide
    <ulink url="https://developer.apple.com/library/archive/documentation/Carbon/Conceptual/SpotlightQuery/Concepts/Introduction.html">
    https://developer.apple.com/library/archive/documentation/Carbon/Conceptual/SpotlightQuery/Concepts/Introduction.html</ulink>
    </para>
  </refsect1>

  <refsect1>
    <title>VERSION</title>

    <para>This man page is part of version 4.24.6 of the Samba suite.</para>
  </refsect1>

  <refsect1>
    <title>AUTHOR</title>

    <para>The original Samba software and related utilities were created by
    Andrew Tridgell. Samba is now developed by the Samba Team as an Open
    Source project similar to the way the Linux kernel is developed.</para>

    <para>The mdsearch manpage was written by Ralph Boehme.</para>
  </refsect1>
</refentry>
